{
  "id": "ctzn.network/destroy-item-method",
  "title": "Destroy item method",
  "description": "Delete a item from a database.",
  "type": "dbmethod",
  "parameters": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "required": ["itemKey", "qty"],
    "properties": {
      "itemKey": {"type": "string"},
      "qty": {"type": "integer", "minimum": 1}
    }
  }
}