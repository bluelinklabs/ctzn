{
  "id": "ctzn.network/methods/community-join",
  "title": "Community join method",
  "description": "Joins a community.",
  "type": "method",
  "parameters": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "required": ["communityDbId"],
    "properties": {
      "communityDbId": {"type": "string"}
    }
  },
  "definition": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "required": ["membershipRecord", "memberRecord"],
    "properties": {
      "membershipRecord": {
        "type": "object",
        "required": ["key", "dbUrl"],
        "properties": {
          "key": {"type": "string"},
          "dbUrl": {"type": "string", "format": "uri"}
        }
      },
      "memberRecord": {
        "type": "object",
        "required": ["key", "dbUrl"],
        "properties": {
          "key": {"type": "string"},
          "dbUrl": {"type": "string", "format": "uri"}
        }
      }
    }
  }
}