{
  "id": "ctzn.network/methods/community-remove-member",
  "title": "Remove member method",
  "description": "Remove a member from a community.",
  "type": "method",
  "parameters": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "required": ["member"],
    "properties": {
      "member": {
        "type": "object",
        "required": ["dbKey"],
        "properties": {
          "dbKey": {
            "type": "string",
            "pattern": "^[0-9a-f]{64}$"
          }
        }
      },
      "ban": {"type": "boolean"},
      "banReason": {"type": "string"}
    }
  },
  "definition": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "properties": {
      "banRecord": {
        "type": "object",
        "required": ["key", "dbUrl"],
        "properties": {
          "key": {"type": "string"},
          "dbUrl": {"type": "string", "format": "uri"}
        }
      }
    }
  }
}