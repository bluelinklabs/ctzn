{
  "id": "ctzn.network/methods/set-muted",
  "title": "Set muted method",
  "description": "Set the 'muted' setting on an account",
  "type": "method",
  "parameters": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "required": ["dbKey", "muted"],
    "properties": {
      "dbKey": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
      "muted": {"type": "boolean"}
    }
  }
}